export type ExerciseT = {
  name: string;
  loadType: string;
  sets: string;
  reps: string;
  load: string;
  unit: string;
  notes: string;
};

export type TemplateT = {
  name: string;
  startDate: any;
  endDate: any;
};
